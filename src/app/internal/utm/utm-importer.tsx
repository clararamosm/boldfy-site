/**
 * @deprecated DELETAR este arquivo no próximo commit (sandbox não tem
 * permissão pra git rm; Clara remove manualmente).
 *
 * Foi usado pra migrar os 3 links UTM do gerador HTML legado em mai/2026.
 * Migração concluída — o componente não é mais montado em lugar nenhum.
 */
export {};
