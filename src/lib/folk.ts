/**
 * DEPRECATED — arquivo morto, manter só pra evitar import-not-found até a
 * Clara rodar `rm` no terminal nativo dela (sandbox sem permissão).
 *
 * Todo o código Folk legacy foi removido em mai/2026. Substituído pela extensão
 * Boldfy futura (ver ADR-010 no spec) + sync único do AC.
 *
 * Quando deletar este arquivo:
 *   rm src/lib/folk.ts
 *
 * Confirma antes que nenhum outro arquivo importa daqui (deveria estar limpo).
 */

export {};
